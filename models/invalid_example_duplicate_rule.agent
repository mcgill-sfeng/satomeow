llm: "gpt-5.4-nano"

Summarize : "text summarizer" {
    input: "A long document"
    behavior: "Summarize the document"
    example {
        input: "doc text"
        commands: ["read"]
        output: "summary"
    }
}

do safety_rule: "Do not hallucinate facts"
do safety_rule: "Duplicate rule"
