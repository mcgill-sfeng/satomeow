llm: "gpt-5.4-nano"

Summarize : "first summarizer" {
    input: "doc"
    behavior: "summarize"
    example {
        input: "doc"
        commands: []
        output: "out"
    }
}

Summarize : "second summarizer" {
    input: "doc"
    behavior: "summarize differently"
    example {
        input: "doc"
        commands: []
        output: "out"
    }
}
